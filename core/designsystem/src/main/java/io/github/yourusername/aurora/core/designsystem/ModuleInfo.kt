/**
 * Design System: Color.kt, Type.kt, Shape.kt, Theme.kt, and reusable
 * components (cards, shelves, buttons) belong here — ported from the
 * Figma export's design tokens (Newsreader/Inter type scale, warm-ivory
 * palette, ceramic-panel glass nav, film grain).
 *
 * Intentionally empty for Task 001. Comes in the next task, before any
 * feature screens are built, matching "Design System exists before any
 * feature" in the architecture doc.
 */
package io.github.yourusername.aurora.core.designsystem
